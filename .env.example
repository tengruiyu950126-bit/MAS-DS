# Shell-environment example only; MAS-DS does not automatically load this file.
# Deterministic modes need neither value. Non-loopback URLs require UI opt-in.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:4b
